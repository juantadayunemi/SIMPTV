# Paquetes adicionales para equipos con GPU NVIDIA
# Primero instalar: pip install -r requirements.txt
# Luego instalar: pip install -r requirements-gpu.txt

# PyTorch con soporte CUDA 11.8 para GPU NVIDIA
torch==2.7.1+cu118
torchvision==0.22.1+cu118
