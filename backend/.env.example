# Django Configuration# Base de datos SQL Server

SECRET_KEY=django-insecure-p08#hw$-d%ufc!j5139jqlu)o_mv_3-*c06l%r)0v!-a-s@tj^DATABASE_URL="sqlserver://jsofuseradmin:1234567890@.:1433;database=UrbiaDb;trustServerCertificate=true"

DEBUG=True

ALLOWED_HOSTS=127.0.0.1,localhost# Seguridad JWT
ALGORITHM="HS256"

# Email Configuration (Gmail SMTP)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password-here
EMAIL_FROM=TrafiSmart <noreply@trafismart.com>
FRONTEND_URL=http://localhost:5174

# CORS
CORS_ORIGINS="http://localhost:5174,http://localhost:3000"
DB_ENGINE=mssql
DB_NAME=UrbiaDb
DB_USER=suusuario
DB_PASSWORD=sucontraseña
DB_HOST=localhost
DB_PORT=1433
DB_TRUST_SERVER_CERTIFICATE=true

# Email Configuration (Gmail SMTP)
# IMPORTANTE: Debes generar un "App Password" de Gmail
# 1. Ve a https://myaccount.google.com/security
# 2. Activa la verificación en 2 pasos
# 3. Busca "App passwords" y genera una para "Mail"
# 4. Usa esa contraseña de 16 caracteres aquí abajo
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=xxxx@unemi.edu.ec
EMAIL_HOST_PASSWORD=xxxx xxxx xxxx xxxx
EMAIL_FROM=TrafiSmart <noreply@trafismart.com>
FRONTEND_URL=http://localhost:5174


# CORS Configuration (Frontend URLs)SENDGRID_API_KEY="your-sendgrid-api-key"

# Add your frontend URLs separated by commasFROM_EMAIL="noreply@yourcompany.com"

FRONTEND_URLS=http://localhost:3000,http://127.0.0.1:3000

# WhatsApp (Twilio)

# File Upload ConfigurationTWILIO_ACCOUNT_SID="your-twilio-account-sid"

MAX_UPLOAD_SIZE=10485760  # 10MB in bytesTWILIO_AUTH_TOKEN="your-twilio-auth-token"

TWILIO_WHATSAPP_NUMBER="whatsapp:+14155238886"

# Logging Level (DEBUG, INFO, WARNING, ERROR, CRITICAL)

LOG_LEVEL=INFO# CORS

CORS_ORIGINS="http://localhost:5173,http://localhost:3000"

# Entity Generator Configuration

ENTITIES_PATH=../../shared/src/entities# Rutas de archivos

AUTO_GENERATE_ON_STARTUP=FalseUPLOAD_PATH="./uploads"
LOG_PATH="./logs"

# Configuración de desarrollo
DEBUG=True
ENVIRONMENT="development"